use yew::prelude::*;

pub(crate) struct Props {
    item: Children,
    padding: Option<Children>,
}

#[function_component(LazyFor)]
pub(crate) fn lazy_for() -> Html {
    html! {
        <>

        </>
    }
}
